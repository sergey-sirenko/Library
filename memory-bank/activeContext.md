# Активный контекст

Этот файл читается после `AGENTS.md`: здесь актуальное состояние и маршруты.
Код и Git имеют приоритет.

## Маршрут по типу задачи

| Задача | Что прочитать |
|---|---|
| Цель, границы или источник требований | `projectbrief.md` |
| Поведение, пользовательский сценарий или UI | `productContext.md` |
| Архитектура, хранение, сеть, безопасность или обновление приложения | `systemPatterns.md` |
| Структура, стек, сборка или тестирование | `techContext.md` |
| Реализация, ревью, документация или Git | `developmentWorkflow.md` |
| Версия, публикация или проверка релиза | `releaseProcess.md` |
| Реализованные возможности, ограничения или бэклог | `progress.md` |

Читайте только строки, относящиеся к задаче. Для сложной задачи допустимо
совместить несколько маршрутов.

## Текущее состояние

- Версия исходников и последний известный стабильный релиз: `1.1.2`.
- Основная ветка: `main`; удалённый репозиторий: `sergey-sirenko/Library`.
- Локальная ветка опережает `origin/main` на один документационный коммит.
- Текущий фокус: рефакторинг инструкций и перенос документации в `docs/`.

## Проверки и блокеры

- Последняя стабильная сборка приложения относится к версии `1.1.2`.
- Ссылки проверены; стартовый контекст меньше 6 КБ;
  `git diff --check` не выявил ошибок.
- `Build-Release.ps1 -AllowDirty` остановился до тестов: запущенное приложение
  удерживает `Prog/Library.exe` и не позволяет компоновщику заменить файл.
- Для полной проверки закройте приложение и повторите команду.

## Существующие изменения рабочего дерева

До начала текущей задачи в `Prog/` уже отсутствовала отслеживаемая runtime-копия
старой инструкции и присутствовали неотслеживаемые `INSTALL.md` и
`Library.exe.old`. Они не относятся к рефакторингу и должны остаться нетронутыми.
